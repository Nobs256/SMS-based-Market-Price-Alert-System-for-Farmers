<?php require_once __DIR__ . '/../partials/header.phtml'; ?>

<?php
if (isset($_SESSION['status'])) {
    $status = $_SESSION['status'];
    echo "<div class='status-message {$status['type']}'>" . htmlspecialchars($status['message']) . "</div>";
    unset($_SESSION['status']);
}
?>

<div class="form-section">
    <h2>Edit Price Entry</h2>
    <form action="edit-price.php" method="post">
        <input type="hidden" name="price_id" value="<?php echo $price['id']; ?>">
        <label for="market_name">Market Name:</label>
        <input type="text" id="market_name" name="market_name" value="<?php echo htmlspecialchars($price['market_name']); ?>" required>

        <label for="price_per_kg">Price per Kg (UGX):</label>
        <input type="number" id="price_per_kg" name="price_per_kg" step="0.01" value="<?php echo $price['price_per_kg']; ?>" required>

        <label for="price_per_sack">Price per Sack (UGX):</label>
        <input type="number" id="price_per_sack" name="price_per_sack" step="0.01" value="<?php echo $price['price_per_sack']; ?>" required>

        <label for="price_date">Price Date:</label>
        <input type="date" id="price_date" name="price_date" value="<?php echo htmlspecialchars($price['price_date']); ?>" required>

        <button type="submit">Update Price & Broadcast</button>
        <a href="dashboard.php?page=prices" class="cancel-btn">Cancel</a>
    </form>
</div>

<?php require_once __DIR__ . '/../partials/footer.phtml'; ?>