<?php 
$pageTitle = "Manage Posts";
include __DIR__ . '/../partials/header.phtml'; 
?>

<div class="container py-4">
    <div class="row">
        <!-- Post Creation Form -->
        <div class="col-lg-4">
            <div class="card shadow-sm border-0 mb-4">
                <div class="card-header bg-white py-3">
                    <h5 class="mb-0 fw-bold">Create New Post</h5>
                </div>
                <div class="card-body">
                    <?php if (isset($_SESSION['status'])): ?>
                        <div class="alert alert-<?= $_SESSION['status']['type'] === 'success' ? 'success' : 'danger' ?> small">
                            <?= $_SESSION['status']['message'] ?>
                            <?php unset($_SESSION['status']); ?>
                        </div>
                    <?php endif; ?>

                    <form action="posts.php" method="POST">
                        <div class="mb-3">
                            <label class="form-label">Title</label>
                            <input type="text" name="title" class="form-control" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Category</label>
                            <select name="category" class="form-select">
                                <option value="news">News</option>
                                <option value="guideline">Guideline</option>
                                <option value="blog">Blog</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Content</label>
                            <textarea name="content" class="form-control" rows="10" required></textarea>
                        </div>
                        <button type="submit" class="btn btn-success w-100">Publish Post</button>
                    </form>
                </div>
            </div>
        </div>

        <!-- Post List -->
        <div class="col-lg-8">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-white py-3">
                    <h5 class="mb-0 fw-bold">Published Content</h5>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th class="ps-4">Title</th>
                                    <th>Category</th>
                                    <th>Date</th>
                                    <th class="text-end pe-4">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php foreach ($posts as $post): ?>
                                <tr>
                                    <td class="ps-4 fw-bold"><?= htmlspecialchars($post['title']) ?></td>
                                    <td><span class="badge bg-light text-dark border"><?= ucfirst($post['category']) ?></span></td>
                                    <td class="small text-muted"><?= date('M d, Y', strtotime($post['created_at'])) ?></td>
                                    <td class="text-end pe-4">
                                        <button class="btn btn-sm btn-outline-danger">Delete</button>
                                    </td>
                                </tr>
                                <?php endforeach; ?>
                                <?php if (empty($posts)): ?>
                                    <tr><td colspan="4" class="text-center py-4 text-muted">No posts found.</td></tr>
                                <?php endif; ?>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<?php include __DIR__ . '/../partials/footer.phtml'; ?>