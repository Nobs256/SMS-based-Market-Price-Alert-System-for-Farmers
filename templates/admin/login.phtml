<?php $title = 'Admin Login'; ?>
<?php require_once __DIR__ . '/../partials/auth_header.phtml'; ?>

<style>
    body {
        background-color: #f4f7f6;
        margin: 0;
        overflow-x: hidden;
    }
    .auth-bg-curve {
        position: fixed;
        top: 0;
        left: 50%;
        width: 200%;
        height: 50vh;
        background-color: #16A085;
        border-radius: 0 0 50% 50%;
        transform: translateX(-50%);
        z-index: -1;
    }
    .auth-container {
        position: relative;
        z-index: 1;
    }
    .auth-bg-text {
        position: fixed;
        bottom: 10vh;
        left: 0;
        width: 100%;
        font-size: 8vw;
        font-weight: 900;
        color: #16A085;
        white-space: nowrap;
        z-index: -2;
        pointer-events: none;
        user-select: none;
        animation: slideText 10s linear infinite;
    }
    @keyframes slideText {
        from { transform: translateX(100%); }
        to { transform: translateX(-100%); }
    }
</style>

<div class="auth-bg-text">SMS MARKET PRICE ALERT SYSTEM</div>

<div class="auth-bg-curve"></div>

<div class="auth-container">
    <div class="auth-card">
        <div class="auth-header">
            <h1>Welcome to the Market Price Alert System</h1>
            <p>Enter your login and password to sign in</p>
        </div>

        <?php
        // Display and clear the status message from the session
        if (isset($_SESSION['status'])) {
            $status = $_SESSION['status'];
            echo "<div class='status-message {$status['type']}'>" . htmlspecialchars($status['message']) . "</div>";
            unset($_SESSION['status']);
        }
        ?>

        <form action="login.php" method="post">
            <label for="username">Username</label>
            <input type="text" id="username" name="username" required autofocus>

            <label for="password">Password</label>
            <input type="password" id="password" name="password" required>

            <button type="submit">Login</button>
        </form>
        <div class="auth-footer">
            <p>Don't have an account? <a href="register.php">Register here</a></p>
        </div>
    </div>
</div>

<?php require_once __DIR__ . '/../partials/auth_footer.phtml'; ?>