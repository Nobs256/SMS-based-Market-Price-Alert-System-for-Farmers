<div class="data-section">
    <div class="section-header">
        <h3>Total Farmers: <?php echo $totalFarmers; ?></h3>
        <div class="export-buttons">
            <a href="export.php?format=csv" class="export-btn">Export All to CSV</a>
            <a href="export.php?format=pdf" class="export-btn">Export All to PDF</a>
        </div>
    </div>
    <table class="data-table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Phone Number</th>
                <th>Language</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($paginatedFarmers as $farmer): ?>
                <tr>
                    <td><?php echo htmlspecialchars($farmer['names']); ?></td>
                    <td><?php echo htmlspecialchars($farmer['phone_number']); ?></td>
                    <td><?php echo htmlspecialchars($farmer['preferred_language']); ?></td>
                    <td>
                        <a href="edit-farmer.php?id=<?php echo $farmer['id']; ?>" class="edit-btn">Edit</a>
                        <form action="dashboard.php?page=farmers" method="post" style="display:inline;" onsubmit="return confirm('Are you sure you want to delete this farmer?');">
                            <input type="hidden" name="farmer_id" value="<?php echo $farmer['id']; ?>">
                            <button type="submit" name="delete_farmer" class="delete-btn">Delete</button>
                        </form>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>

    <!-- Pagination Controls -->
    <?php if ($totalPages > 1): ?>
    <nav class="pagination">
        <?php if ($currentPage > 1): ?>
            <a href="?page=farmers&p=<?php echo $currentPage - 1; ?>">« Previous</a>
        <?php endif; ?>

        <?php for ($i = 1; $i <= $totalPages; $i++): ?>
            <a href="?page=farmers&p=<?php echo $i; ?>" class="<?php echo ($i == $currentPage) ? 'active' : ''; ?>">
                <?php echo $i; ?>
            </a>
        <?php endfor; ?>

        <?php if ($currentPage < $totalPages): ?>
            <a href="?page=farmers&p=<?php echo $currentPage + 1; ?>">Next »</a>
        <?php endif; ?>
    </nav>
    <?php endif; ?>
</div>