<div class="data-section">
    <table class="data-table">
        <thead>
            <tr>
                <th>Market</th>
                <th>Price (UGX)</th>
                <th>Price Date</th>
                <th>Entry Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php if (empty($allPrices)): ?>
                <tr>
                    <td colspan="4" style="text-align: center;">No prices found. <a href="dashboard.php?page=add-price">Add one now</a>.</td>
                </tr>
            <?php else: ?>
                <?php foreach ($allPrices as $price): ?>
                    <tr>
                        <td><?php echo htmlspecialchars($price['market_name']); ?></td>
                        <td><?php echo number_format($price['price']); ?></td>
                        <td><?php echo htmlspecialchars($price['price_date']); ?></td>
                        <td><?php echo htmlspecialchars($price['created_at']); ?></td>
                        <td>
                            <a href="edit-price.php?id=<?php echo $price['id']; ?>" class="edit-btn">Edit</a>
                            <form action="dashboard.php?page=prices" method="post" style="display:inline;" onsubmit="return confirm('Are you sure you want to delete this price entry?');">
                                <input type="hidden" name="price_id" value="<?php echo $price['id']; ?>">
                                <button type="submit" name="delete_price" class="delete-btn">Delete</button>
                            </form>
                        </td>
                    </tr>
                <?php endforeach; ?>
            <?php endif; ?>
        </tbody>
    </table>
</div>