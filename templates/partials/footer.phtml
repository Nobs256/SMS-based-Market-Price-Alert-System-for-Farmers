<?php
// Re-detect layout context for the footer
$isAdminPath = \strpos($_SERVER['PHP_SELF'], '/admin/') !== false;
$isLoginPage = \strpos($_SERVER['PHP_SELF'], 'login.php') !== false;
$showAdminSidebar = $isAdminPath && !$isLoginPage;
?>
        </main>
    <?php if ($showAdminSidebar): ?>
    </div> <!-- Close app-container -->
    <?php else: ?>
        <footer class="bg-dark text-white py-5 mt-5">
            <div class="container">
                <div class="row align-items-center">
                    <div class="col-md-6 text-center text-md-start">
                        <h5 class="fw-bold">Rubanda Potato Hub</h5>
                        <p class="text-secondary small mb-0">&copy; <?= date('Y') ?> Market Price Alert System. All rights reserved.</p>
                    </div>
                </div>
            </div>
        </footer>
    <?php endif; ?>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Chart.js Library -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</body>
</html>