<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold mb-0">Welcome, <?= htmlspecialchars($_SESSION['farmer_names']) ?></h2>
            <p class="text-muted">Manage your Irish potato stock and sales</p>
        </div>
        <a href="add-listing.php" class="btn btn-success shadow-sm">
            <i class="bi bi-plus-lg me-2"></i>Add New Stock
        </a>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-white py-3">
                    <h5 class="mb-0 fw-bold">My Active Listings</h5>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th class="ps-4">Variety</th>
                                    <th>Quantity</th>
                                    <th>Price/Unit</th>
                                    <th>Location</th>
                                    <th>Status</th>
                                    <th class="text-end pe-4">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php if (empty($myListings)): ?>
                                    <tr>
                                        <td colspan="6" class="text-center py-5 text-muted">You haven't added any potatoes for sale yet.</td>
                                    </tr>
                                <?php else: ?>
                                    <?php foreach ($myListings as $listing): ?>
                                    <tr>
                                        <td class="ps-4 fw-bold text-success"><?= htmlspecialchars($listing['variety']) ?></td>
                                        <td><?= number_format($listing['quantity_available']) ?> <?= htmlspecialchars($listing['unit']) ?>s</td>
                                        <td>UGX <?= number_format($listing['price_per_unit']) ?></td>
                                        <td><?= htmlspecialchars($listing['location']) ?></td>
                                        <td>
                                            <span class="badge rounded-pill <?= $listing['status'] == 'available' ? 'bg-success' : 'bg-secondary' ?>">
                                                <?= ucfirst($listing['status']) ?>
                                            </span>
                                        </td>
                                        <td class="text-end pe-4">
                                            <button class="btn btn-sm btn-outline-primary">Edit</button>
                                        </td>
                                    </tr>
                                    <?php endforeach; ?>
                                <?php endif; ?>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>