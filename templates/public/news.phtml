<div class="container py-5">
    <div class="text-center mb-5">
        <h1 class="fw-bold">Agricultural Resources</h1>
        <p class="text-muted">Guidelines, news, and best practices for potato farming in Rubanda.</p>
    </div>

    <div class="row mb-4">
        <div class="col-12 text-center">
            <div class="btn-group shadow-sm">
                <a href="<?= $siteUrl ?>/public/news.php" class="btn btn-sm <?= !isset($_GET['category']) ? 'btn-success' : 'btn-outline-success' ?>">All</a>
                <a href="<?= $siteUrl ?>/public/news.php?category=news" class="btn btn-sm <?= ($_GET['category'] ?? '') == 'news' ? 'btn-success' : 'btn-outline-success' ?>">News</a>
                <a href="<?= $siteUrl ?>/public/news.php?category=guideline" class="btn btn-sm <?= ($_GET['category'] ?? '') == 'guideline' ? 'btn-success' : 'btn-outline-success' ?>">Guidelines</a>
            </div>
        </div>
    </div>

    <div class="row g-4">
        <?php if (empty($posts)): ?>
            <div class="col-12 text-center py-5">
                <p class="text-muted">No posts available at the moment.</p>
            </div>
        <?php else: ?>
            <?php foreach ($posts as $post): ?>
                <div class="col-md-6 col-lg-4">
                    <div class="card h-100 shadow-sm border-0 transition-hover">
                        <div class="card-body">
                            <span class="badge bg-light text-success border border-success mb-2"><?= ucfirst($post['category']) ?></span>
                            <h4 class="card-title fw-bold"><?= htmlspecialchars($post['title']) ?></h4>
                            <p class="card-text text-secondary small">
                                <?= substr(strip_tags($post['content']), 0, 150) ?>...
                            </p>
                        </div>
                        <div class="card-footer bg-white border-0 py-3">
                            <div class="d-flex justify-content-between align-items-center">
                                <small class="text-muted">
                                    <i class="bi bi-calendar3 me-1"></i> <?= date('M d, Y', strtotime($post['created_at'])) ?>
                                </small>
                                <a href="<?= $siteUrl ?>/public/post-detail.php?id=<?= $post['id'] ?>" class="btn btn-sm btn-link text-success p-0 fw-bold text-decoration-none">
                                    Read Article &rarr;
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        <?php endif; ?>
    </div>
</div>